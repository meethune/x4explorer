# x4explorer
A standalone web application that provides a unified, browsable interface for X4 game data and mod analysis. Takes an x4cat SQLite database as input and serves it as a read-only web interface.
